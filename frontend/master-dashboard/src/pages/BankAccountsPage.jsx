import React,{useEffect,useState} from 'react';
import PageHeader from '../components/ui/PageHeader';import Button from '../components/ui/Button';import Input from '../components/ui/Input';import Modal from '../components/ui/Modal';import Alert from '../components/ui/Alert';
import {listBrands} from '../api/brands';import {listBankAccounts,createBankAccount,updateBankAccount,deleteBankAccount} from '../api/bankAccounts';
const empty={brand_id:'',bank_name:'',account_number:'',account_holder:'',instructions:'',is_active:true,sort_order:0};
export default function BankAccountsPage(){const [items,setItems]=useState([]),[brands,setBrands]=useState([]),[form,setForm]=useState(empty),[editing,setEditing]=useState(null),[open,setOpen]=useState(false),[error,setError]=useState('');const load=()=>Promise.all([listBankAccounts(),listBrands()]).then(([a,b])=>{setItems(a||[]);setBrands(b||[])}).catch(()=>setError('Data rekening gagal dimuat.'));useEffect(()=>{load()},[]);const save=async(e)=>{e.preventDefault();try{const payload={...form,brand_id:Number(form.brand_id),sort_order:Number(form.sort_order)};editing?await updateBankAccount(editing,payload):await createBankAccount(payload);setOpen(false);setForm(empty);setEditing(null);load()}catch(err){setError(err.response?.data?.error||'Rekening gagal disimpan.')}};return <div className="space-y-6"><PageHeader title="Rekening Bank" actionLabel="Tambah Rekening" onAction={()=>{setForm(empty);setEditing(null);setOpen(true)}}/>{error&&<Alert variant="error">{error}</Alert>}<div className="grid gap-4 md:grid-cols-2">{items.map(a=><article key={a.id} className="rounded-xl border border-neutral-200 bg-white p-5"><div className="flex justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wider text-primary-700">{a.brand_name}</p><h2 className="mt-2 text-lg font-bold text-neutral-900">{a.bank_name}</h2><p className="mt-1 font-mono text-base text-neutral-700">{a.account_number}</p><p className="mt-1 text-sm text-neutral-500">a.n. {a.account_holder}</p></div><span className={`h-fit rounded-full px-2 py-1 text-xs font-bold ${a.is_active?'bg-success-50 text-success-700':'bg-neutral-100 text-neutral-500'}`}>{a.is_active?'Aktif':'Nonaktif'}</span></div><div className="mt-5 flex gap-3 border-t border-neutral-100 pt-4"><button className="text-sm font-semibold text-primary-700" onClick={()=>{setEditing(a.id);setForm({...a,brand_id:String(a.brand_id),instructions:a.instructions||''});setOpen(true)}}>Edit</button><button className="text-sm font-semibold text-danger-700" onClick={async()=>{if(confirm('Hapus rekening ini?')){await deleteBankAccount(a.id);load()}}}>Hapus</button></div></article>)}</div><Modal isOpen={open} onClose={()=>setOpen(false)} title={editing?'Edit Rekening':'Tambah Rekening'}><form onSubmit={save} className="space-y-4"><label className="block text-sm font-medium">Brand<select required value={form.brand_id} onChange={e=>setForm({...form,brand_id:e.target.value})} className="mt-1 w-full rounded-lg border border-neutral-300 px-3 py-2"><option value="">Pilih brand</option>{brands.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></label><Input label="Nama Bank" value={form.bank_name} onChange={e=>setForm({...form,bank_name:e.target.value})} required/><Input label="Nomor Rekening" value={form.account_number} onChange={e=>setForm({...form,account_number:e.target.value.replace(/\D/g,'')})} required/><Input label="Atas Nama" value={form.account_holder} onChange={e=>setForm({...form,account_holder:e.target.value})} required/><Input label="Instruksi (opsional)" value={form.instructions} onChange={e=>setForm({...form,instructions:e.target.value})}/><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/> Aktif dan tampil di portal</label><div className="flex justify-end gap-2"><Button type="button" variant="ghost" onClick={()=>setOpen(false)}>Batal</Button><Button type="submit">Simpan</Button></div></form></Modal></div>}
